"""
Local browser rendering: markdown-it-py + Pygments served over a localhost
http.server; never sends content off-machine (spec §9.5).
"""

# TODO(HOPPUS): implement browser rendering in a later story.
