"""
Configuration loading: global ``~/.config/hoppus/config.yaml`` deep-merged
with per-vault ``<vault>/.hoppus/config.yaml`` overrides (spec §12).
"""

# TODO(HOPPUS): implement config schema loading and merging in a later story.
