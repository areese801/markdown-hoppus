[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "markdown-hoppus"
version = "0.0.1"
description = "A terminal-first, TUI Markdown knowledge base — Obsidian-compatible, standalone, MIT-licensed. (Placeholder stub release.)"
readme = "README.md"
requires-python = ">=3.11"
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Adam Reese" }]
keywords = ["markdown", "obsidian", "tui", "notes", "knowledge-base", "terminal"]
classifiers = [
    "Development Status :: 1 - Planning",
    "Environment :: Console",
    "Intended Audience :: Developers",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Text Processing :: Markup :: Markdown",
]

# Three interchangeable entry points into the same CLI (spec §1, §10).
[project.scripts]
hoppus = "hoppus:main"
mark = "hoppus:main"
hop = "hoppus:main"

[tool.hatch.build.targets.wheel]
packages = ["src/hoppus"]

[tool.ruff]
line-length = 88
